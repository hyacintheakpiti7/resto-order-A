import type { ReactNode } from "react";
const TONES: Record<string, string> = { slate: "from-slate-50 to-white text-slate-900", amber: "from-amber-50 to-white text-amber-900", emerald: "from-emerald-50 to-white text-emerald-900", sky: "from-sky-50 to-white text-sky-900", violet: "from-violet-50 to-white text-violet-900", rose: "from-rose-50 to-white text-rose-900" };
export function StatCard({ label, value, hint, icon, tone = "slate" }: { label: string; value: ReactNode; hint?: ReactNode; icon?: ReactNode; tone?: keyof typeof TONES }) { return <div className={`rounded-2xl border border-slate-200 bg-gradient-to-br ${TONES[tone]} p-4 shadow-sm`}><div className="flex items-start justify-between gap-2"><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{label}</p>{icon && <span className="text-xl leading-none">{icon}</span>}</div><p className="mt-2 text-2xl font-bold tabular-nums">{value}</p>{hint && <p className="mt-1 text-xs text-slate-500">{hint}</p>}</div>; }
