import type { DashboardPayload } from "./types";

export function DashboardAlerts({ alerts, loading }: { alerts: DashboardPayload["alerts"]; loading: boolean }) {
  return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-bold text-slate-900">Alertes à traiter</h2><p className="mt-1 text-sm text-slate-500">Les priorités opérationnelles de la journée.</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{alerts.length} alerte(s)</span></div><div className="mt-5 grid gap-3 md:grid-cols-3">{loading ? <div className="col-span-full py-4 text-center text-sm text-slate-400">Mise à jour du tableau de bord…</div> : alerts.length === 0 ? <div className="col-span-full rounded-2xl bg-emerald-50 px-4 py-5 text-center text-sm font-medium text-emerald-700">✓ Tout est sous contrôle pour le moment.</div> : alerts.map((alert) => <div key={alert.title} className={`rounded-2xl border p-4 ${alert.tone === "rose" ? "border-rose-200 bg-rose-50" : "border-amber-200 bg-amber-50"}`}><p className={`text-sm font-bold ${alert.tone === "rose" ? "text-rose-800" : "text-amber-800"}`}>⚠ {alert.title}</p><p className={`mt-1 text-xs leading-5 ${alert.tone === "rose" ? "text-rose-700" : "text-amber-700"}`}>{alert.message}</p></div>)}</div></section>;
}
